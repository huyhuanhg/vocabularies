import { FC } from "react";
import Container from "./Question.style";
import QuestionProps from "./Question.props";

const Question: FC<QuestionProps> = ({ reviewId }) => {
  return (
    <Container>
    </Container>
  );
};

export default Question;
